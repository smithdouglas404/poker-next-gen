import { RewardsMarketplace } from "@/features/rewards/RewardsMarketplace";

export const metadata = {
  title: "Rewards · High Rollers Club",
  description: "Redeem your points for real sponsor rewards — travel, dining, recreation & more.",
};

export default function RewardsPage() {
  return <RewardsMarketplace />;
}
