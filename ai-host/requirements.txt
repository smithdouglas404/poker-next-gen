# Deliberately not pinning aiohttp/loguru/pipecatcloud here — two attempts at
# guessing their versions both conflicted with what pipecat-ai actually
# requires (aiohttp~=3.11.12, loguru~=0.7.3), and pipecatcloud + aiohttp are
# already present in the dailyco/pipecat-base image this builds on top of
# (confirmed in the build log: "Requirement already satisfied"). bot.py
# imports both aiohttp and loguru directly — they're guaranteed present
# either way, just not re-pinned to a version that might not exist.
pipecat-ai[daily,anthropic,deepgram,cartesia,silero]==0.0.66
