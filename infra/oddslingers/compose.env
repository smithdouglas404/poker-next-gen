# Overrides for OddSlingers when run inside poker-next-gen docker compose.
POSTGRES_HOST=oddslingers-postgres
REDIS_HOST=oddslingers-redis
ALLOWED_HOSTS=localhost,127.0.0.1,oddslingers.l,oddslingers-nginx
DEFAULT_HOST=localhost
