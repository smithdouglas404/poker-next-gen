{
  "name": "openproject-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "OpenProject MCP review agent: reads work-package changes via the OpenProject MCP server, reviews them with Claude, and posts insight/recommendation/methodology back via the OpenProject REST API. Hourly + on-demand, configurable interval.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "express": "^4.21.2",
    "falkordb": "^6.2.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/node": "^20.19.9",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
